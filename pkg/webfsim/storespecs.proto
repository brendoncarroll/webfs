syntax = "proto3";

package webfs;
option go_package = "webfsim";

message HTTPStoreSpec {
    string prefix = 1;
    string endpoint = 2;

    map<string, string> headers =3;
}

message IPFSStoreSpec {
    string endpoint = 1;
}
